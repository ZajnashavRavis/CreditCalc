package kreditcalc;
// Начало кода программы
// Код не очень большой так как выводится средствами GUI и создается окно фреймом
// Произведем импорт библиотеки функций
import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;
import javax.swing.*;

// объявляем класс KreditCalc
public class KreditCalc implements  ActionListener{
    // Текстовые импуты в виде первый, второй, третий, четвертый
        JTextField input1 = new JTextField(8); 
        JTextField input2 = new JTextField(8);
        JTextField input3 = new JTextField(8);
        JTextField input4 = new JTextField(8);
        // Кнопка результата
        JButton Rezultat;
        // Кнопка очистки
        JButton Clearing;
        // Выводы результатов
        JLabel vuvod1;
        JLabel vuvod2;
        JLabel vuvod3;
        JLabel vuvod4;
    public static void main(String[] args) {
        KreditCalc calc = new KreditCalc();
    }
